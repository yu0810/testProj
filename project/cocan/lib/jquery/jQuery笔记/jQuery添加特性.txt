.attr()
.attr(传入属性名)：获取属性的值；
.attr(属性名，属性值)：设置属性的值；
.attr(属性名，函数值)：设置属性的函数值；
.attr(attributes)：给指定元素设置多个属性值，
即：｛属性名一：“属性值一”，属性名二：“属性值二”｝
即用对象名值对的方式传入，名字无需引号，属性需要加上引号，名和值之间用分号：
隔开，名值对之间用逗号，隔开。
注意和css方法传入对象的区别